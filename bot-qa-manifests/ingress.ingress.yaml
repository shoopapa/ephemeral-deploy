apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  name: ingress-c8490260
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: Deployment
                port:
                  number: 3000
            path: /bots/Deployment
            pathType: Prefix
    - http:
        paths:
          - backend:
              service:
                name: Service
                port:
                  number: 3000
            path: /bots/Service
            pathType: Prefix
    - http:
        paths:
          - backend:
              service:
                name: app
                port:
                  number: 3000
            path: /bots/app
            pathType: Prefix
    - http:
        paths:
          - backend:
              service:
                name: head-603c1c0-c8248daa
                port:
                  number: 3000
            path: /bots/head-603c1c0-c8248daa
            pathType: Prefix
