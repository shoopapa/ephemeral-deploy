apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  name: ingress-c8490260
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: bot-c81d80cb
                port:
                  number: 3000
            path: /bots/bot-c81d80cb
            pathType: Prefix
    - http:
        paths:
          - backend:
              service:
                name: bot
                port:
                  number: 3000
            path: /bots/bot
            pathType: Prefix
